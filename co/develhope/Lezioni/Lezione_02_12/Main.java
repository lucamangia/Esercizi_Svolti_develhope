package co.develhope.Lezioni.Lezione_02_12;

public class Main {

    /*Crea una classe astratta Lavoratore con:

    Attributi: nome (String), stipendio (double)
    Metodo astratto: calcolaPaga() che deve essere implementato nelle classi derivate.
    Crea due classi derivate: Impiegato e LiberoProfessionista.
    Impiegato: ha un salario fisso (un attributo stipendioMensile) e
    implementa calcolaPaga() restituendo semplicemente il valore di stipendioMensile.
    LiberoProfessionista: ha un attributo numeroOreLavorate (double) e tariffaOraria (double). La classe deve implementare
    calcolaPaga() restituendo il prodotto di numeroOreLavorate e tariffaOraria.*/

    /*Descrizione: Crea due interfacce: Lettore (con il metodo leggi()) e Scrittore (con il metodo scrivi()).
    Crea una classe Persona che implementa entrambe le interfacce. Implementa i metodi leggi() e scrivi() in Persona. */

    /*Creare una classe astratta per rappresentare un'entità base.
      Creare classi derivate che estendono la classe astratta, utilizzando modificatori come protected e static.
      Usare final per evitare l'override di metodi o l'estensione di classi.
      Gestire variabili e metodi statici*/


}


