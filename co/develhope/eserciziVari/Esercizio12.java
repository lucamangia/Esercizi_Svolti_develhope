package co.develhope.eserciziVari;

import java.util.Scanner;

public class Esercizio12 {
    public static void main(String[] args) {

        Scanner scan = new Scanner(System.in);

        /*int n1 = scan.nextInt();
        int n2 = scan.nextInt();
        int n3 = scan.nextInt();

        int valoreMaggiore = (n1 > n2) ? ((n1 > n3) ? n1 : n3) : ((n2 > n3) ? n2 : n3);
        System.out.println(valoreMaggiore);*/

        /*int n1 = 30;

        String Colore = (n1 <= 50 ) ? ((n1 <20) ? "rosso" : "giallo") : ("verde");*/

        /*int eta = scan.nextInt();
        String categoria = (eta<18) ? "junior" : ((eta<=35) ? ("Senior") : ("Master"));
        System.out.println(categoria);*/

        /*int anno = scan.nextInt();

        String annoBisestile = ((anno % 4 == 0 && anno % 100 !=0) || (anno % 400 == 0)) ? "anno bs" : "anno non bs";
        System.out.println(annoBisestile);*/

        //Scrivi un programma che assegni punti bonus a un punteggio base:
        //Se il punteggio base è maggiore di 80, aggiungi 20 punti bonus.
        //Se è tra 50 e 80, aggiungi 10 punti bonus.
        //Altrimenti, non aggiungere bonus.

        /*int punteggioBase = scan.nextInt();

        int Bonus = (punteggioBase > 80) ? (punteggioBase+20) : ((punteggioBase>= 50 && punteggioBase<= 80) ? (punteggioBase+10) : (punteggioBase+0));

        System.out.println(Bonus);*/

        /*int numero = scan.nextInt();*/

        /*int divisibile = (((numero%2==0) || (numero%3==0)) || ((numero%2==0) && (numero%3==0))) ? (numero/2) : (numero/3);
        */
        /*String divisibile = numero % 2 == 0 ? numero % 3 == 0 ? "2 e 3" : "2" : numero % 3 == 0 ? "3" : "né due né 3";*/

        /*int[] arr = {1, 2, 3, 4, 5};
        int somma= 0;

        for ( int i : arr){
            somma += i;*/

        /*String[] parole = {"Java", "for-each", "esempio", "programmazione"};
        String parolaLunga;

        for (String i : parole){
            parolaLunga=parole[0];
            if (parole[0].length() < parole[1].length()){
                parolaLunga=parole[1];
            }
            System.out.println(parolaLunga);
        }*/

        //Scrivi un programma che conti quante volte un numero specifico appare in un array di interi.

        /*int[] numeri = {1, 2, 3, 4, 2, 5, 2};
        int numeroDaContare = 2;
        int count = 0;

        for (int numero : numeri ){
            if (numero == numeroDaContare){
                count+
       }
    }*/






















    }

}
